import tw, { styled } from 'twin.macro';

const Button = styled.button(
  tw`px-4 py-2 font-semibold text-white bg-indigo-600 rounded-full focus:outline-none hover:bg-indigo-700`,
);

export default Button;
