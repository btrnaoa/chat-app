{
  "name": "chat-app",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "concurrently \"yarn workspace server start\" \"yarn workspace client start\"",
    "heroku-postbuild": "yarn && yarn workspace client build"
  },
  "devDependencies": {
    "concurrently": "^6.0.0"
  }
}
